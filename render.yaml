services:
  - type: web
    name: eco-cycle-pay
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /var/data
    disk:
      name: eco-cycle-pay-data
      mountPath: /var/data
      sizeGB: 1
